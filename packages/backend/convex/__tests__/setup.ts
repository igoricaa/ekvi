/**
 * Convex Test Utilities
 *
 * This file is for Convex-specific test helpers only.
 * It gets compiled by Convex deployment, so avoid Node.js APIs.
 *
 * Note: crons.ts is excluded from test module loading (see test.setup.ts).
 * Test cron functions directly by calling them as mutations.
 */

// Future: Add Convex-specific test helpers here
// Example:
// export async function createTestUser(t: ConvexTestingHelper) { ... }
// export async function createTestVideo(t: ConvexTestingHelper) { ... }

export {};
